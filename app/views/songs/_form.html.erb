<%= form_for(song) do |f| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tid %>
    <%= f.text_field :tid %>
  </div>

  <div class="field">
    <%= f.label :mxmid %>
    <%= f.number_field :mxmid %>
  </div>

  <div class="field">
    <%= f.label :lyrics_hash %>
    <%= f.text_area :lyrics_hash %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
